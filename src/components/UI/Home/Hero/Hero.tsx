import React from 'react';

const Hero = () => {
    return (
        <div className='relative w-full h-screen flex justify-center flex-col'>
            hero
        </div>
    );
};

export default Hero;